# Build, Test and Source Map

C++20 is required. `src/` contains bounded types, traits and adapters; `docs/` defines boundedness, containers and conversion contracts; `tests/` includes runtime and compile-time/negative validation; `demos/` illustrate supported usage.
