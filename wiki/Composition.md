# Composition

EDP-BoundedTypes is primarily value/container infrastructure rather than a runtime provider domain. It consumes ByteOperations behaviour from lower layers where required and exposes extension through traits/adapters rather than a runtime registry.
